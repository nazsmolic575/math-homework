function getRandomInt(max) {
  return Math.floor(Math.random() * max);
}

// Example usage:
const randomNumber = getRandomInt(10); // Generate a random number between 0 and 9
console.log(`The random number is: ${randomNumber}`);
